# Ticketing System

Simple Flask and MariaDB app for managing helpdesk tickets. It supports ticket creation, updates, comments, filtering, and basic reporting.

Quick start: install dependencies with `pip install -r requirements.txt`, create the database with `schema.sql`, load sample data from `seed.sql`, configure `.env`, and run `python app.py`.
